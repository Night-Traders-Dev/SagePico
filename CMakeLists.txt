cmake_minimum_required(VERSION 3.13)
include($ENV{PICO_SDK_PATH}/external/pico_sdk_import.cmake)
project(hello_sage)
pico_sdk_init()
add_executable(hello_sage hello.c)
target_include_directories(hello_sage PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
pico_add_extra_outputs(hello_sage)